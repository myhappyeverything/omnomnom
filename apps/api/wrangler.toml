# Full bindings (D1 / KV / R2 / Cron) are configured in Stage 6.
name = "purple-api"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "development"
